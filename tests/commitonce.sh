#!/bin/bash

# Create a bundlefs repository with an initial commit with an empty FS.
mkdir -p bundles/i0/tree
mkdir pieces/

# Mount it and find nothing

# Make and commit changes

# Find a new bundle with the changed pieces
